# iOS client placeholder

This folder is reserved for the eventual SwiftUI client. The shared KMP business logic is designed so the iOS app can consume the same tournament engine, state model, entitlements, and serialization contracts without rewriting the rules.
